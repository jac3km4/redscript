
let while_ = (cond) -> {
    while cond {}
};

let if_ = (cond) -> {
    if cond {}
};

let for_ = (arr) -> {
    for el in arr {}
};

let switch_ = (scrutinee) -> {
    switch scrutinee {
        case "":
    }
};
